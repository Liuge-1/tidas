<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.tidaswebmanagement.mapper.ClazzMapper">

    <select id="list" resultType="org.example.tidaswebmanagement.pojo.Clazz">
        SELECT c.*, e.name master_name
        FROM clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                AND c.name LIKE concat('%',#{name},'%')
            </if>
            <if test="subject != null">
                AND c.subject = #{subject}
            </if>
        </where>
        ORDER BY c.id ASC
    </select>

    <select id="listAll" resultType="org.example.tidaswebmanagement.pojo.Clazz">
        SELECT id,name FROM clazz ORDER BY id ASC
    </select>

    <select id="countStudentByClazzId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM student WHERE clazz_id = #{clazzId}
    </select>

    <select id="getClazzDetailById" resultType="org.example.tidaswebmanagement.pojo.Clazz">
        SELECT c.*, e.name master_name
        FROM clazz c
                 LEFT JOIN emp e ON c.master_id = e.id
        WHERE c.id = #{id}
    </select>
</mapper>